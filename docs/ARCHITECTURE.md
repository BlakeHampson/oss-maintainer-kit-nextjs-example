# Architecture Notes

This file exists to capture the high-level boundaries that reviewers should keep in mind.

## Record the parts that matter

- critical routes and user flows
- server components versus client components
- middleware and auth boundaries
- server actions or route handlers
- external APIs, storage, and background processing

## Why this helps

A short architecture note makes it easier to review risky changes without rediscovering the same context in every pull request.
