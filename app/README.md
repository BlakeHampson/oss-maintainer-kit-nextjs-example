# App Surface Notes

This folder stands in for the app routes and UI surface of a real Next.js project.

Maintainers should document:

- which routes are critical user flows
- which areas use server components, client components, or server actions
- where auth or middleware behavior can affect navigation
